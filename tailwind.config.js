// Used for tailwind VS Code extension only

/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./client/**/*.{html,js,.templ}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
